# NOTE: THIS VERSION OF REQUIREMENTS.TXT IS FOR TORCH ON CUDA/GPU

# CUDA supported versions (i think)
# must be run with this when installing: pip install -r Requirements.txt --extra-index-url https://download.pytorch.org/whl/cu121
torch==2.4.0+cu121
torchvision==0.19.0+cu121

# ALT - should be usable as-is, but the two packages and the command above have been verified to work.
# GPU-enabled PyTorch (CUDA 12.1)
#torch @ https://download.pytorch.org/whl/cu121/torch-2.4.0%2Bcu121-cp310-cp310-win_amd64.whl
#torchvision @ https://download.pytorch.org/whl/cu121/torchvision-0.19.0%2Bcu121-cp310-cp310-win_amd64.whl

# These were needed on my desktop cus numpy was too new and typing-extensions was too old for evogym
numpy==1.26.4
typing-extensions>=4.14.0

# Spiking Neural Network packages
snntorch
#norse

# standard stuff
matplotlib
pandas
gymnasium

# CPU (default) version
#torch
#torchvision

#for cartpole env
#gymnasium[classic-control]

imageio[ffmpeg]

# either use with python3.12 (or newer) or include the following list of packages instead while ignoring objectrl
#objectrl

# packages needed on python3.10 cus objectrl (as-is) only works with python3.12+ (NOTE: SYNTAX MODIFICATIONS MUST BE MADE TO ACCOMODATE PYTHON3.10 FOR OBJECTRL CODE)
tyro
tensordict
torchrl
tqdm
PyYAML
dm_env
dm_control

# works with python3.10, not with newer versions
evogym

# the PyQt6 package allows matplotlib to show plot gui when in non-interactive-window-mode or whatever idk just include it
PyQt6 

# idk what IPython is used for so its left out for now
#IPython
